"""
import json
from googleapiclient.discovery import build

from gator.auth.credentials import load_credentials
from gator.utils import print_helpers as ph

def compute_list_instances(project_id):
def compute_inject_startup_script(project_id):
def compute_add_ssh_key(project_id):
def compute_impersonate(project_id):

    try:

    except Exception as ex:
        ph.print_error("{}".format(ex))
        print()
"""